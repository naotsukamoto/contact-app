name: Automatic Release
on:
  push:
    branch:
      - main
jobs:
  release:
    runs-on: macos-latest
    steps:
      - name: test
        run: npm run test
      - name: release
        run: npm run deploy
